kind: GitRepo
apiVersion: fleet.cattle.io/v1alpha1
metadata:
  name: op1-config
  namespace: fleet-default
spec:
  repo: https://github.com/camuzard/fleet
  branch: master
  targetNamespace: dev
  paths:
  - operators/op1/config
  targets:
  - name: dev
    clusterGroup: op1